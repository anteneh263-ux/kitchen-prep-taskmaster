# Copy to .env (never commit .env). Offline dev needs none of these.
GOOGLE_API_KEY=
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=europe-north1
RESTAURANT_LAT=59.91
RESTAURANT_LON=10.75
# Set to "firestore" to use Firestore instead of the local JSON store.
KP_STORE=
