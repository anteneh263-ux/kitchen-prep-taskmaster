Gemini has exactly two bounded jobs: propose dish demand and write the operational briefing. It never owns an ingredient, inventory, shortfall, order, or delivery quantity. The forecast must match the known menu, authoritative date and guest count, strict data types, and a dishes-per-cover safety band. If the proposal fails, a deterministic baseline takes over and the dashboard visibly marks the degraded path.
